{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Current File",
      "cwd": "${workspaceRoot}",
      "program": "${file}",
      "request": "launch",
      "type": "dart"
    }
  ]
}